# Structure générale du projet

## Run le programme :
Il faut créer l'environnement en suivant la procedure ci-dessous:
- run le pipeline vlm exemple
- uv pip install -r requirements.txt
- cp .env.example .env  # and fill in the values
- configurer le client (proxy), avec le certificat SSL de votre machine

Lancer le venv .globalvenvmatt:
- source .globalvenvmatt/bin/activate

## Pense-bête :
ctrl + § = pour commenter multi-line<br/>
pull le projet le matin pour éviter les conflits entre les branches<br/>
